// OSR (Off-Screen Rendering) was a JetBrains-specific feature.
// This component is kept as a stub since the project no longer supports JetBrains IDEs.
// It renders nothing and exists only for backward compatibility with existing imports.

const OSRContextMenu = () => null;

export default OSRContextMenu;
